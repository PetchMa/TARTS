{% set name = "tarts" %}
{% set version = "0.0.0a3" %}  # Update this to your current version
{% set python_min = "3.8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/PetchMa/TARTS/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 789b7899faffa1b8c2550477a5a0a7d810161f342f62c397d186f4f18946fdb2

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation"

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - numpy
    - scipy
    - matplotlib
    - pytorch
    - pytorch-lightning
    - pyyaml
    - torchvision
    - astropy
    - pandas
    - gitpython

test:
  requires:
    - python {{ python_min }}
  imports:
    - TARTS
  commands:
    - python -c "import TARTS; print('Package imported successfully')"
    - python -c "from TARTS import NeuralActiveOpticsSys; print('Core package imported successfully')"

about:
  home: https://github.com/PetchMa/TARTS
  license: MIT
  license_file: LICENSE
  summary: "I like tarts"

extra:
  recipe-maintainers:
    - PetchMa

{% set name = "tarts" %}
{% set version = "0.0.0a4" %}  # Update this to your current version
{% set python_min = "3.8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/PetchMa/TARTS/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 44189755bd476d5de1c65a6dc1748454661bc6c9c93adf371f391cf197b85f9b

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation"

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - numpy
    - scipy
    - matplotlib-base
    - pytorch
    - pytorch-lightning
    - pyyaml
    - torchvision
    - astropy-base
    - pandas
    - gitpython

test:
  requires:
    - python {{ python_min }}
  imports:
    - TARTS
  commands:
    - python -c "import TARTS; print('Package imported successfully')"

about:
  home: https://github.com/PetchMa/TARTS
  license: MIT
  license_file: LICENSE
  summary: "Neural Active Optics System for LSST telescope"

extra:
  recipe-maintainers:
    - PetchMa
